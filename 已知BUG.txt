步骤  
打开目录(打开的时候数据目录会变成 相同路径  需要制定额外的  再手动打开 数据目录)
打开数据目录


最后一张图片
需要切换一下其他图片 才会保存

自定义标注框颜色的功能  有问题 删了 以后再加

偶尔打开会无法放大缩小，重新打开试试，还不行多试几次。。。
也可能会一开始行突然不行了，因为代码写的很乱换了好几种实现导致变成了这样
有空还要重写

在其他窗口按键盘  也会触发按键操作  比如 ad 切换上下图片........

偶尔会标注框 显示不完全(实际是正常的 只是看不见  移动一下 或者 调整一下缩放  就能看见 如果一直不显示  可能就是其他bug)

偶尔切换图片会莫名其妙崩溃,还没找到解决办法(保存后载入下一张时崩溃.数据不会丢)

标注框错位修复了，但是  拉框模式  没有 还是会有轻微的错位 不影响使用。

灰度图片，不会把xml 里depth设置成1，现在默认标记为3 还没研究怎么检测是否灰度图  labelimg打开一下好像就正常了

还没有加入标记难度的功能。如果需要，暂时用labelimg标记。

修复了已标框错位的位置，但是修复方式错了,导致拉框的时候 实际位置是红色十字的中心(跟鼠标左上角偏了  不影响使用  只是有丢丢别扭)

打开目录后可能会白屏，拖动一下就正常了，需要激活窗口







